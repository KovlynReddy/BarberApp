@model BarberAppDLL.Models.ViewModels.CreateBarberViewModel

@{
    ViewData["Title"] = "ViewBarber";
}

<h1>@Model.BarberName</h1>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BarberEmail)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BarberEmail)
        </dd>

    </dl>

    <a asp-controller="Booking" asp-action="Create" class="btn btn-primary" > Book </a>
</div>

